-- This script will query the exact Roles we just dynamically inserted!

USE GPMS_Desktop;
GO

-- Let's view the fully populated Entity Framework schema data!
SELECT * FROM [Roles];
GO

SELECT * FROM [__EFMigrationsHistory];
GO
